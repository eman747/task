export default class CartItem {
  constructor(quantity, price, title, sum, images, id) {
    this.quantity = quantity;
    this.price = price;
    this.title = title;
    this.sum = sum;
    this.images = images;
    this.id = id;
  }
}
